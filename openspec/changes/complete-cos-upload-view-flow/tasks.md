## 1. 持久化与运行时接线

- [x] 1.1 梳理 `internal/ingestion` 当前依赖的仓储接口，并补齐基于 PostgreSQL 的导入会话、资产、对象引用和识别状态读写实现
- [x] 1.2 在 API 与 Worker 启动路径中接入 PostgreSQL 仓储，移除运行时默认回落到 `NewMemoryStore()` 的行为
- [x] 1.3 为 PostgreSQL 仓储补充覆盖导入确认、服务重启后读取和发现接口查询的集成测试

## 2. Redis 队列与 Worker 消费

- [x] 2.1 为现有增强任务抽象实现 Redis 持久队列适配层，支持任务入队、出队、重试与基础命名空间隔离
- [x] 2.2 在上传确认成功后改为写入 Redis 队列，并确保任务载荷包含资产标识、阶段信息和幂等键
- [x] 2.3 在 `cmd/worker` 中实现真实消费循环，执行派生资源生成、元数据提取与状态持久化更新
- [x] 2.4 为 Worker 消费与重复任务重试补充幂等性和重启恢复测试

## 3. COS Provider 设置与上传状态机

- [x] 3.1 实现 `PUT /api/v1/settings/providers/{providerName}`，支持更新 COS 运行时配置并返回脱敏后的配置摘要
- [x] 3.2 在 Provider 设置更新流程中加入 bucket 连通性、权限和私有读约束校验，失败时保持旧配置不变
- [x] 3.3 收紧上传确认流程中的对象校验逻辑，覆盖单次上传与 multipart 上传的确认数据验证

## 4. Web 上传与查看闭环

- [x] 4.1 在 `apps/web/pages/import.vue` 中实现基于上传票据的 multipart 上传流程，支持分片上传、etag 收集与完成确认
- [x] 4.2 调整上传后的页面状态管理，确保确认成功的资产能够在总览、时间线和详情页中被查询到并显示处理中状态
- [x] 4.3 为缩略图未就绪场景提供受控占位或回退展示，避免把“处理中”误呈现为“上传失败”

## 5. 端到端验证

- [x] 5.1 扩展后端 HTTP 测试流，覆盖 PostgreSQL + Redis 下的上传、确认、Worker 消费和查看流程
- [ ] 5.2 增加 Web 侧对单次上传与 multipart 上传分支的联调验证
- [ ] 5.3 在真实 Go 运行环境和真实 COS 配置下完成一次“上传到 COS 并在 Web 查看”的端到端验收记录
