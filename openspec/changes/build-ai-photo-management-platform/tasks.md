## 1. 基础设施

- [x] 1.1 初始化 Nuxt Web、Go API、Go Worker、数据库迁移与查询目录的基础项目结构
- [x] 1.2 建立 OpenAPI 接口骨架、错误模型、分页模型与前端 TypeScript Client 生成流程
- [x] 1.3 增加数据库、队列、对象存储 Provider 和 AI Provider 的统一配置加载能力，并支持密钥引用、密文存储或环境注入
- [x] 1.4 定义资产、对象引用、导入会话、识别阶段、相册和备份记录的 PostgreSQL Schema、扩展启用与迁移脚本
- [x] 1.5 建立 `pgx` / `sqlc` 风格的查询组织方式，补齐核心查询定义与生成流程
- [x] 1.6 增加 PostgreSQL、Redis 和腾讯云 COS 示例配置的本地开发依赖、启动脚本与健康检查，对象存储模拟器仅作为可选联调方案
- [x] 1.7 增加腾讯云 COS bucket CORS、前端直传域名白名单、对象 key 前缀和私有读配置示例
- [x] 1.8 增加日志、指标、trace 和任务载荷的敏感字段脱敏规则，覆盖密钥、预签名 URL、GPS、OCR 等高风险内容

## 2. Provider 契约

- [x] 2.1 定义统一的存储 Provider 接口，覆盖对象写入、读取、删除、列举、预签名、Metadata、对象存在性校验和分片上传
- [x] 2.2 实现以腾讯云 COS 为首版目标的对象存储接入与启动健康检查，并确保默认部署路径可直接接入现有 COS
- [x] 2.3 实现服务端签发预签名上传、固定对象 key 分配和分片上传初始化/完成校验，本地 S3 兼容模拟器仅作为可选联调方案
- [x] 2.4 定义统一的 AI Provider 接口，覆盖 caption、OCR、embedding 和能力声明
- [x] 2.5 实现基于隐私策略、任务能力、重试处理和降级状态记录的 Provider 路由规则
- [x] 2.6 预留本地 AI sidecar 或独立推理服务的调用边界、超时与错误分类模型
- [x] 2.7 增加 Provider 凭据脱敏、最小权限校验和公开读误配置检测
- [x] 2.8 为上传与下载实现单对象、短时效、最小权限的签名策略

## 3. 访问控制与审计

- [x] 3.1 建立统一认证会话与主体上下文，覆盖 Web 与 API 的照片库访问边界
- [x] 3.2 对时间线、搜索、详情、下载、导出、Provider 设置和隐私策略接口实施授权检查
- [x] 3.3 为浏览器发起的高风险变更请求增加 CSRF 或等价同站保护，并对导出和密钥修改增加近期认证
- [x] 3.4 增加登录、导出、批量下载、Provider 配置变更、隐私策略变更和调试数据访问的审计事件

## 4. 导入流水线

- [x] 4.1 实现导入会话、服务端生成预签名和上传确认入口，覆盖 Web 前端直传 COS 与批量导入场景
- [x] 4.2 在确认阶段通过存储抽象执行对象存在性、大小和 etag 或等价校验，并记录不可变的源对象引用
- [x] 4.3 在导入过程中增加基于内容哈希的精确去重和基于感知相似度的候选检测
- [x] 4.4 生成缩略图和预览图等派生资源，并更新资产的浏览就绪状态
- [x] 4.5 为上传链路增加不透明对象 key、单会话上传凭据和过期重签逻辑

## 5. 识别增强与索引

- [x] 5.1 实现可幂等重试的后台任务和阶段迁移，覆盖元数据提取、AI 识别、索引更新和部分失败恢复
- [x] 5.2 增加 EXIF 解析、拍摄时间归一化、设备信息提取和 GPS 照片的地理反解
- [x] 5.3 增加 caption、tag、OCR 和 embedding 任务，并将 Provider 返回结果归一为内部识别记录
- [ ] 5.4 构建 PostgreSQL 全文和向量索引更新流程，确保部分识别完成的资产也能被检索到，并显示正确状态
- [x] 5.5 实现按隐私策略控制 GPS、OCR、caption、embedding 的执行、持久化和默认返回范围，并为原始 Provider 响应调试保留增加 TTL

## 6. 检索与浏览体验

- [ ] 6.1 实现基于统一资产和地点元数据的时间线与地点浏览查询
- [x] 6.2 实现把结构化过滤、OCR 文本、caption 或 tag 文本以及 embedding 相似度整合到一起的混合搜索
- [ ] 6.3 增加基于 Nuxt 的重复照片审查、收藏和精选相册管理的最小可用界面
- [x] 6.4 在发现层响应里暴露识别状态和备份状态，确保新上传资产在处理过程中也能保持可见
- [x] 6.5 为列表、详情和下载链路落实受控缩略图访问、字段最小返回和稳定公网 URL 禁用

## 7. 备份与可迁移性

- [ ] 7.1 实现二级备份复制任务，并跟踪备份目标侧的对象引用
- [ ] 7.2 在标记资产已受保护之前，对复制后的资产执行完整性校验
- [ ] 7.3 生成可迁移导出包和 manifest，支持按相册、时间范围或整库导出
- [ ] 7.4 实现读取导出 manifest 的恢复规划逻辑，用于重建统一资产元数据要求
- [ ] 7.5 为导出工件增加审计、短时受控访问或加密归档能力，并支持脱敏 manifest

## 8. 验证与交付准备

- [ ] 8.1 增加 OpenAPI 契约、Provider 适配器、导入去重、识别状态迁移和混合搜索基础排序的自动化测试
- [ ] 8.2 增加覆盖 Web 上传、识别、检索、备份复制和导出生成的端到端测试
- [ ] 8.3 增加认证授权、CSRF 保护、短时签名、密钥脱敏、导出审计和隐私策略的安全测试
- [ ] 8.4 增加任务失败、Provider 健康、备份滞后、索引进度和异常访问审计的可观测性
- [ ] 8.5 编写部署、回滚、OpenAPI 生成、腾讯云 COS 配置、隐私策略、密钥治理、审计查询和备份校验的运维文档
