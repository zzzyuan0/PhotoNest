## 1. 基础设施

- [ ] 1.1 创建共享领域模型、API 运行时和后台 Worker 运行时的初始项目结构
- [ ] 1.2 增加数据库、队列、对象存储 Provider 和 AI Provider 的配置加载能力
- [ ] 1.3 定义资产、对象引用、导入会话、识别阶段、相册和备份记录的 PostgreSQL Schema 与迁移脚本
- [ ] 1.4 增加 PostgreSQL、Redis 和对象存储模拟器的本地开发依赖与启动脚本

## 2. Provider 契约

- [ ] 2.1 定义统一的存储 Provider 接口，覆盖对象写入、读取、删除、列举、预签名、Metadata 和分片上传
- [ ] 2.2 实现至少一个 S3 兼容后端和一个厂商自有 OSS 后端的存储适配器与启动健康检查
- [ ] 2.3 定义统一的 AI Provider 接口，覆盖 caption、OCR、embedding 和能力声明
- [ ] 2.4 实现基于隐私策略、任务能力、重试处理和降级状态记录的 Provider 路由规则

## 3. 导入流水线

- [ ] 3.1 实现上传和批量导入入口，创建导入会话与统一资产记录
- [ ] 3.2 通过存储抽象持久化原始文件，并记录不可变的源对象引用
- [ ] 3.3 在导入过程中增加基于内容哈希的精确去重和基于感知相似度的候选检测
- [ ] 3.4 生成缩略图和预览图等派生资源，并更新资产的浏览就绪状态

## 4. 识别增强与索引

- [ ] 4.1 实现可幂等重试的后台任务和阶段迁移，覆盖元数据提取、AI 识别、索引更新和部分失败恢复
- [ ] 4.2 增加 EXIF 解析、拍摄时间归一化、设备信息提取和 GPS 照片的地理反解
- [ ] 4.3 增加 caption、tag、OCR 和 embedding 任务，并将 Provider 返回结果归一为内部识别记录
- [ ] 4.4 构建 PostgreSQL 全文和向量索引更新流程，确保部分识别完成的资产也能被检索到，并显示正确状态

## 5. 检索与浏览体验

- [ ] 5.1 实现基于统一资产和地点元数据的时间线与地点浏览查询
- [ ] 5.2 实现把结构化过滤、OCR 文本、caption 或 tag 文本以及 embedding 相似度整合到一起的混合搜索
- [ ] 5.3 增加重复照片审查、收藏和精选相册管理的接口或最小可用界面
- [ ] 5.4 在发现层响应里暴露识别状态和备份状态，确保新上传资产在处理过程中也能保持可见

## 6. 备份与可迁移性

- [ ] 6.1 实现二级备份复制任务，并跟踪备份目标侧的对象引用
- [ ] 6.2 在标记资产已受保护之前，对复制后的资产执行完整性校验
- [ ] 6.3 生成可迁移导出包和 manifest，支持按相册、时间范围或整库导出
- [ ] 6.4 实现读取导出 manifest 的恢复规划逻辑，用于重建统一资产元数据要求

## 7. 验证与交付准备

- [ ] 7.1 增加 Provider 适配器、导入去重、识别状态迁移和混合搜索基础排序的自动化测试
- [ ] 7.2 增加覆盖上传、识别、检索、备份复制和导出生成的端到端测试
- [ ] 7.3 增加任务失败、Provider 健康、备份滞后和索引进度的可观测性
- [ ] 7.4 编写部署、回滚以及 Provider 配置、隐私策略和备份校验的运维文档
