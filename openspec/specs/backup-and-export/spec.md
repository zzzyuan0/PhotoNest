# backup-and-export Specification

## Purpose
TBD - created by archiving change build-ai-photo-management-platform. Update Purpose after archive.
## Requirements
### Requirement: 照片库支持独立的二级备份目标
系统 MUST 允许运维人员配置一个独立于主照片库存储之外的二级备份目标，并通过定时或事件驱动方式把原始资产及所需 manifest 复制到该目标。

#### Scenario: 新资产进入备份复制队列
- **WHEN** 某个资产成功写入主照片库
- **THEN** 系统把该资产及其必要元数据加入复制计划，发送到已配置的二级备份目标

### Requirement: 备份完整性必须可校验
系统 MUST 跟踪备份状态，并在把资产标记为已受保护之前，校验备份副本是否与源资产身份及完整性元数据一致。

#### Scenario: 备份副本通过校验
- **WHEN** 复制到二级备份目标的流程完成
- **THEN** 系统比对身份信息和完整性元数据，只有在副本匹配时才把资产的备份状态标记为已验证

### Requirement: 用户可以导出可迁移的照片库包
系统 MUST 生成包含原始资产、派生元数据和机器可读 manifest 的导出包，以便照片库可以在运行中服务之外进行迁移或恢复。

#### Scenario: 用户导出一个精选相册
- **WHEN** 用户请求导出某个选定相册或时间范围
- **THEN** 系统生成一个归档包和 manifest，标识其中每个资产、对应元数据，以及原始文件与派生资源之间的关系

### Requirement: 导出必须经过显式授权、审计并受时效或加密保护
系统 MUST 将导出视为高风险操作，对导出请求执行认证授权，并记录审计事件；导出工件在交付时必须采用短时受控下载、加密归档包或等价的保护方式，而不是提供可长期传播的明文下载地址。

#### Scenario: 用户发起整库导出
- **WHEN** 已认证用户请求导出整库或大范围资产
- **THEN** 系统校验其导出权限，记录包含主体、范围、时间和结果的审计事件，并以受时效限制或受加密保护的方式交付导出工件

#### Scenario: 过期导出链接被再次访问
- **WHEN** 某个导出工件的下载入口已经超过允许时效
- **THEN** 系统拒绝继续提供该工件，并要求重新生成或重新授权

### Requirement: manifest 必须支持敏感字段最小暴露
系统 MUST 在保证恢复可行性的前提下，支持对导出 manifest 和备份说明数据应用最小暴露原则，避免在不需要的场景中扩散原始 GPS 坐标、完整 OCR 文本、内部对象引用或调试保留数据。

#### Scenario: 运维为外部核对生成导出说明
- **WHEN** 运维人员需要向外部系统或人工流程提供导出内容说明，但不需要完整恢复能力
- **THEN** 系统可以生成省略高敏感字段的说明或脱敏 manifest，而不是强制暴露全部恢复元数据

### Requirement: 恢复流程可以基于导出 manifest 重建照片库元数据
系统 MUST 在每个导出 manifest 中保留足够的元数据，以便在恢复流程中重建统一资产记录、Provider 无关的对象引用以及识别增强结果。

#### Scenario: 使用导出 manifest 规划恢复
- **WHEN** 运维人员在恢复准备阶段检查一个导出 manifest
- **THEN** manifest 必须明确列出重建所选照片库内容所需的资产、元数据字段和备份工件

