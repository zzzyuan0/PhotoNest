# AGENTS

## 语言规则

- 默认使用中文与用户沟通，包括说明、计划、Review 结论、实现说明与后续建议。
- 默认使用中文撰写仓库内文档，包括 OpenSpec 的 proposal、design、tasks 与 specs 内容。
- 如果某个工具、协议或 schema 要求保留固定英文关键字或结构标记，则保留这些英文标记，正文内容改用中文，避免破坏工具兼容性。
- 代码标识符、API 名称、环境变量、数据库字段、能力 ID 等技术键名可继续使用英文。
- 如果用户在某一轮明确要求使用其他语言，则以该轮用户要求为准。
